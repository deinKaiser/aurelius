{
  "name": "aurelius",
  "version": "0.0.2",
  "description": "Aurelius is a Node.js backend framework that embodies the philosophy of minimalism and embraces developer freedom.",
  "scripts": {
    "clean": "tsc -b --clean packages",
    "build:prod": "tsc -b -v packages",
    "prerelease": "gulp copy-misc",
    "publish:prod": "npm run clean && npm run build:prod && lerna publish --force-publish --exact -m \"chore(@aurelius-js) publish %s release\"",
    "lint": "eslint 'packages/**/**.ts'",
    "test": "mocha packages/**/*.spec.ts"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/deinKaiser/aurelius.git"
  },
  "author": "Vadym Yarema",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/deinKaiser/aurelius/issues"
  },
  "homepage": "https://github.com/deinKaiser/aurelius#readme",
  "devDependencies": {
    "lerna": "^7.3.0",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "reflect-metadata": "^0.1.13",
    "tslib": "^2.6.2"
  }
}
